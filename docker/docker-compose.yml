version: "3"
services:
  jenkins:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GIT_PASSWORD=${GIT_PASSWORD}
    volumes:
      - jenkins_home:/var/jenkins_home
volumes:
  jenkins_home:

credentials:
  system:
    domainCredentials:
      - credentials:

          # Git credentials (existing)
          - usernamePassword:
              id: git-creds
              username: balajivb25
              password: "${GIT_PASSWORD}"

          # Docker Hub credentials (NEW)
          - usernamePassword:
              id: dockerhub-creds
              username: balajivb25
              password: "${DOCKERHUB_TOKEN}"

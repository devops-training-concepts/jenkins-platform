credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              id: git-creds
              username: balajivb25
              password: "${GIT_PASSWORD}"
